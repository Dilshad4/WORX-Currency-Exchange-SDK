# حزمة تطوير WORX لصرف العملات

حزمة تطوير TypeScript احترافية لواجهة برمجة تطبيقات WORX v1 لصرف العملات. احصل على أسعار صرف العملات في الوقت الفعلي مع ميزات شاملة تشمل التخزين المؤقت الذكي والتحويل دون اتصال والدعم متعدد اللغات.

[![npm version](https://badge.fury.io/js/exapi-currency-sdk.svg)](https://www.npmjs.com/package/exapi-currency-sdk)
[![TypeScript](https://img.shields.io/badge/TypeScript-Ready-blue.svg)](https://www.typescriptlang.org/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

**🌍 الدعم متعدد اللغات**: [English](./README.md) | [کوردی](./README-ku.md)

## المميزات

- 🚀 **دعم كامل لـ TypeScript** - تعريفات أنواع كاملة و IntelliSense
- 💰 **أسعار صرف في الوقت الفعلي** - أكثر من 17 عملة مدعومة منها الدينار العراقي والدولار الأمريكي واليورو والجنيه الإسترليني
- 👀 **قوائم مراقبة العملات** - راقب عملات محددة مع تتبع شخصي
- 📊 **البيانات التاريخية والتحليلات** - اتجاهات الأسعار والتغيرات والتحليلات الشاملة
- 🔄 **إعادة المحاولة التلقائية** - منطق إعادة المحاولة المدمج مع التأخير التدريجي
- 🚦 **حد معدل ذكي** - التعامل التلقائي مع حدود المعدل مع الوعي بالاشتراك
- 🎯 **إدارة الاشتراكات** - دعم خطط مجانية وبرو وبلاتينية
- 🔐 **مصادقة آمنة** - مصادقة رمز Bearer مع معالجة الأخطاء
- 📱 **متعدد المنصات** - يعمل بسلاسة في Node.js والمتصفحات الحديثة
- 💾 **تخزين مؤقت ذكي** - تخزين مؤقت ذكي مع TTL قابل للتكوين لتحسين حصص API
- 🔄 **استطلاع في الوقت الفعلي** - استطلاع تلقائي مع فترات مدارة واستعادة الأخطاء
- 💱 **تحويل دون اتصال** - حول العملات باستخدام الأسعار المخزنة مؤقتاً بدون استدعاءات API
- 🌍 **دعم متعدد اللغات** - أسماء العملات ورسائل الخطأ بالإنجليزية والكردية والعربية
- ⚡ **محسن الأداء** - حجم حزمة أدنى مع دعم tree-shaking
- 🛡️ **مقاومة الأخطاء** - معالجة شاملة للأخطاء مع رموز خطأ مفصلة

## التثبيت

```bash
npm install exapi-currency-sdk
```

```bash
yarn add exapi-currency-sdk
```

```bash
pnpm add exapi-currency-sdk
```

## البدء السريع

```typescript
import WorxClient from 'exapi-currency-sdk';

// تهيئة العميل
const client = new WorxClient({
  apiToken: 'your-api-token-here'
});

// الحصول على جميع أسعار الصرف المتاحة
const rates = await client.getRates();
console.log(rates);

// الحصول على عملة محددة مع التاريخ
const eurData = await client.getCurrency('EUR');
console.log(eurData.currency.price, eurData.currency.trend);

// إضافة عملة إلى قائمة المراقبة
await client.addWatchedCurrency('GBP');

// الحصول على العملات المراقبة
const watched = await client.getWatchedCurrencies();
console.log(watched);
```

## العملات المدعومة

- **USD** - الدولار الأمريكي
- **EUR** - اليورو
- **GBP** - الجنيه البريطاني
- **TRY** - الليرة التركية
- **IRR** - الريال الإيراني
- **NOK** - الكرونة النرويجية
- **SEK** - الكرونة السويدية
- **JOD** - الدينار الأردني
- **SAR** - الريال السعودي
- **AED** - الدرهم الإماراتي
- **CAD** - الدولار الكندي
- **AUD** - الدولار الأسترالي
- **CHF** - الفرنك السويسري
- **DKK** - الكرونة الدنماركية
- **QAR** - الريال القطري
- **KWD** - الدينار الكويتي
- **IQD** - الدينار العراقي

## مرجع API

### تكوين العميل

```typescript
const client = new WorxClient({
  apiToken: 'your-token',        // مطلوب: رمز API الخاص بك
  baseUrl: 'https://api.worx.dev', // اختياري: عنوان URL أساسي مخصص للـ API
  timeout: 30000,                // اختياري: مهلة الطلب بالميلي ثانية (افتراضي: 30000)
  retries: 3,                    // اختياري: عدد المحاولات (افتراضي: 3)
  retryDelay: 1000,              // اختياري: التأخير بين المحاولات بالميلي ثانية (افتراضي: 1000)
  cache: {                       // اختياري: إعدادات التخزين المؤقت
    enabled: true,               // تفعيل التخزين المؤقت
    ttl: 300000                  // 5 دقائق TTL
  }
});
```

### الطرق الرئيسية

#### `getRates(options?: RequestOptions)`

احصل على جميع أسعار الصرف بناءً على تفضيلات المراقبة الخاصة بالمستخدم.

```typescript
const rates = await client.getRates();

// بنية الاستجابة
interface RatesResponse {
  rates: Array<{
    currency: string;
    name: string;
    flag: string;
    price: number;
    sell: number;
    purchase: number;
    usd: number;
    lastUpdated: string;
    isWatched: boolean;
    watchedAt?: string;
    watchId?: string;
  }>;
  pagination: {
    total: number;
    count: number;
  };
  filters: {
    isWatchingMode: boolean;
    watchedCount: number;
    description: string;
  };
}
```

#### `getCurrency(currency: SupportedCurrency, options?: RequestOptions)`

احصل على معلومات مفصلة لعملة محددة بما في ذلك التاريخ والتحليلات.

```typescript
const eurData = await client.getCurrency('EUR');

// الاستجابة تشمل:
// - currency: معلومات العملة المفصلة مع السعر والاتجاه والتغيرات
// - history: مصفوفة من النقاط السعرية التاريخية
// - analytics: أعلى وأدنى ومتوسط الأسعار وعدد نقاط البيانات
```

### الاستخدام المتقدم

#### تكوين التخزين المؤقت الذكي

```typescript
// تكوين إعدادات التخزين المؤقت
const client = new WorxClient({
  apiToken: 'your-token',
  cache: {
    enabled: true,
    ttl: 300000  // 5 دقائق TTL للتخزين المؤقت
  }
});

// مسح التخزين المؤقت عند الحاجة
client.clearCache();

// التحقق من توفر البيانات المخزنة مؤقتاً
const rates = await client.getRates(); // يستخدم التخزين المؤقت إذا كان متاحاً
```

#### الاستطلاع في الوقت الفعلي

```typescript
// بدء استطلاع تحديثات الأسعار
client.startPolling({
  interval: 60000,  // استطلاع كل دقيقة
  currencies: ['EUR', 'GBP', 'TRY'], // اختياري: عملات محددة
  onUpdate: (rates) => {
    console.log('أسعار جديدة مستلمة:', rates);
  },
  onError: (error) => {
    console.error('خطأ في الاستطلاع:', error.message);
  }
});

// إيقاف الاستطلاع
client.stopPolling();

// فحص حالة الاستطلاع
const isPolling = client.isPolling();
```

#### تحويل العملات دون اتصال

```typescript
// تحويل العملات باستخدام الأسعار المخزنة مؤقتاً (بدون استدعاء API)
const convertedAmount = client.convert(100, 'USD', 'EUR');
if (convertedAmount !== null) {
  console.log(`100 USD = ${convertedAmount} EUR`);
} else {
  console.log('التحويل غير متاح - الأسعار غير مخزنة مؤقتاً');
}
```

#### أسماء العملات متعددة اللغات

```typescript
// الحصول على أسماء العملات بلغات مختلفة
const eurName = client.getCurrencyName('EUR', 'en'); // "Euro"
const eurNameKu = client.getCurrencyName('EUR', 'ku'); // "یۆرۆ"
const eurNameAr = client.getCurrencyName('EUR', 'ar'); // "اليورو"

// الاستخدام في تطبيقك
const displayName = client.getCurrencyName('IQD', 'ar'); // "الدينار العراقي"
```

## الأدوات المساعدة

تتضمن حزمة التطوير دوال مساعدة مفيدة:

```typescript
import { 
  validateCurrencyCode, 
  formatCurrency, 
  calculatePercentageChange,
  getCurrencySymbol,
  getSupportedCurrencies,
  roundCurrencyValue
} from 'exapi-currency-sdk';

// التحقق من صحة رموز العملات
const isValid = validateCurrencyCode('EUR'); // true

// تنسيق قيم العملات برموز صحيحة
const formatted = formatCurrency(1234.5678, 'USD'); // $1234.5678
const formatted2 = formatCurrency(1500, 'IQD'); // 1500.0000 ع.د

// حساب التغيرات المئوية
const change = calculatePercentageChange(105, 100); // 5

// الحصول على رموز العملات
const symbol = getCurrencySymbol('EUR'); // €
const symbolArabic = getCurrencySymbol('SAR'); // ﷼

// الحصول على جميع العملات المدعومة
const currencies = getSupportedCurrencies(); // ['USD', 'EUR', 'GBP', ...]

// تقريب القيم بدقة محددة للعملة
const rounded = roundCurrencyValue(1234.56789, 'USD'); // 1234.5679 (4 منازل عشرية)
const roundedIQD = roundCurrencyValue(1234.56789, 'IQD'); // 1235 (بدون منازل عشرية)
```

## معالجة الأخطاء

توفر حزمة التطوير معلومات خطأ مفصلة:

```typescript
import { WorxError } from 'exapi-currency-sdk';

try {
  const rates = await client.getRates();
} catch (error) {
  if (error instanceof WorxError) {
    console.log('رمز الخطأ:', error.code);
    console.log('رمز الحالة:', error.statusCode);
    console.log('الرسالة:', error.message);
    console.log('التفاصيل:', error.details);
    
    // معلومات تحديد المعدل
    if (error.code === 'RATE_LIMIT_EXCEEDED') {
      console.log('إعادة المحاولة بعد:', error.retryAfter, 'ثانية');
      console.log('المتبقي:', error.rateLimitRemaining);
    }
  }
}
```

## رموز الخطأ

- `UNAUTHORIZED` - رمز API غير صالح
- `RATE_LIMIT_EXCEEDED` - تم تجاوز حد المعدل، تحقق من retryAfter
- `SUBSCRIPTION_LIMIT_EXCEEDED` - تم الوصول لحد الاشتراك اليومي
- `VALIDATION_ERROR` - معاملات إدخال غير صالحة
- `CURRENCY_NOT_FOUND` - العملة غير متاحة
- `WATCH_LIMIT_EXCEEDED` - تم الوصول للحد الأقصى من العملات المراقبة
- `WATCH_NOT_FOUND` - العملة المراقبة غير موجودة
- `ACCESS_DENIED` - الوصول للعملة مرفوض (ليست في قائمة المراقبة)
- `TIMEOUT_ERROR` - انتهت مهلة الطلب
- `NETWORK_ERROR` - مشكلة في الاتصال بالشبكة
- `INTERNAL_SERVER_ERROR` - خطأ في الخادم

## تحديد المعدل

تتعامل حزمة التطوير تلقائياً مع حدود المعدل:

- **الخطة المجانية**: 24 استدعاء API يومياً (طلب واحد في الساعة)
- **الخطة البرو**: 1500 استدعاء API يومياً (حوالي 62 في الساعة)
- **الخطة التجارية**: 4000 استدعاء API يومياً (حوالي 166 في الساعة)
- **الخطة البلاتينية**: 8640 استدعاء API يومياً (6 في الدقيقة، 360 في الساعة)

معلومات حد المعدل متاحة في رؤوس الاستجابة وتفاصيل الخطأ.

## خطط الاشتراك

### الخطة المجانية - مجاناً
- **استدعاءات API**: 24 يومياً (واحد في الساعة)
- **Webhooks**: غير متاحة
- **الدعم**: دعم البريد الإلكتروني
- **مثالية لـ**: الاختبار والمشاريع الشخصية الصغيرة

### الخطة البرو - 4.95$/شهر
- **استدعاءات API**: 1500 يومياً
- **Webhooks**: غير متاحة
- **الدعم**: دعم البريد الإلكتروني
- **مثالية لـ**: المشاريع الصغيرة والمتوسطة

### الخطة التجارية - 9.95$/شهر
- **استدعاءات API**: 4000 يومياً
- **Webhooks**: نقطة webhook واحدة
- **الدعم**: دعم البريد الإلكتروني والدردشة المباشرة
- **مثالية لـ**: الشركات النامية والتطبيقات الإنتاجية

### الخطة البلاتينية - 24.95$/شهر
- **استدعاءات API**: 8640 يومياً (6 في الدقيقة)
- **Webhooks**: 5 نقاط webhook
- **الدعم**: دعم 24/7 بالبريد الإلكتروني والدردشة والهاتف
- **مثالية لـ**: التطبيقات المؤسسية والخدمات عالية الحجم

### الخطة المخصصة - اتصل للحصول على السعر
- **استدعاءات API**: مخصصة حسب احتياجاتك
- **Webhooks**: مخصصة حسب احتياجاتك
- **الدعم**: دعم الهاتف والبريد الإلكتروني والدردشة
- **تسعير مخصص**: مُفصل حسب متطلباتك
- **مثالية لـ**: المنظمات ذات الاحتياجات الخاصة

## نصائح الأداء

### تحسين استخدام API للخطط المجانية

```typescript
// استخدم التخزين المؤقت بفعالية لتقليل استدعاءات API
const client = new WorxClient({
  apiToken: 'your-free-plan-token',
  cache: { enabled: true, ttl: 600000 } // 10 دقائق للخطط المجانية
});

// دمج العمليات عند الإمكان
const [rates, account] = await Promise.all([
  client.getRates(),
  client.getAccountInfo()
]);
```

### التطبيقات في الوقت الفعلي

```typescript
// استخدم الاستطلاع للوحات المباشرة
client.startPolling({
  interval: 120000, // دقيقتان للخطط المجانية
  currencies: ['USD', 'EUR', 'GBP'], // راقب عملات محددة
  onUpdate: (rates) => updateUI(rates)
});

// طبق نهج offline-first
const convertedAmount = client.convert(amount, 'USD', 'EUR');
if (convertedAmount === null) {
  // احتياطي لاستدعاء API في حالة فقدان التخزين المؤقت
  const eurData = await client.getCurrency('EUR');
  // استخدم السعر الطازج للتحويل
}
```

## أفضل الممارسات

1. **إدارة التخزين المؤقت**: فعّل التخزين المؤقت لتحسين استخدام الحصة، خاصة على الخطط المجانية
2. **معالجة الأخطاء**: تعامل دائماً مع `WorxError` لتجربة مستخدم مناسبة
3. **تحديد المعدل**: راقب `rateLimitRemaining` في الاستجابات لتجنب الوصول للحدود
4. **الدعم دون اتصال**: استخدم طريقة `convert()` للحسابات دون اتصال
5. **متعدد اللغات**: استخدم `getCurrencyName()` لعرض العملات المحلية
6. **تنظيف الموارد**: اتصل بـ `client.destroy()` عند الانتهاء لتنظيف فترات الاستطلاع

## دعم المتصفح

تعمل حزمة التطوير في المتصفحات الحديثة مع دعم fetch. للمتصفحات الأقدم، ضمّن polyfill للـ fetch:

```typescript
// للمتصفحات الأقدم
import 'whatwg-fetch';
import WorxClient from 'exapi-currency-sdk';

const client = new WorxClient({ apiToken: 'your-token' });
```

## المساهمة

1. اعمل fork للمستودع
2. أنشئ فرع الميزة الخاص بك (`git checkout -b feature/amazing-feature`)
3. اعتمد تغييراتك (`git commit -m 'Add amazing feature'`)
4. ادفع للفرع (`git push origin feature/amazing-feature`)
5. افتح طلب سحب

## الرخصة

هذا المشروع مرخص تحت رخصة MIT - راجع ملف [LICENSE](LICENSE) للتفاصيل.

## الدعم

- 📧 البريد الإلكتروني: support@worx.dev
- 📖 التوثيق: https://docs.worx.dev
- 🐛 المشاكل: https://github.com/worx/currency-sdk/issues
- 💬 ديسكورد: https://discord.gg/worx

## سجل التغيير

### v1.0.0
- الإصدار الأولي
- دعم كامل لـ TypeScript
- تغطية كاملة لـ API v1
- تحديد المعدل ومعالجة الأخطاء
- أدوات مساعدة شاملة
- دعم المتصفح و Node.js
- تخزين مؤقت ذكي واستطلاع في الوقت الفعلي
- تحويل دون اتصال ودعم متعدد اللغات